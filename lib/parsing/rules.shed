module shed/compiler/parsing/rules

members ::
    tokenOfType rules.tokenOfType
    token rules.token
    map rules.map
    firstOf rules.firstOf
    sequence rules.sequence
    zeroOrMore rules.zeroOrMore
    zeroOrMoreWithSeparator rules.zeroOrMoreWithSeparator
    optional rules.optional

import lop/rules
